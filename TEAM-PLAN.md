# צוות הסוכנים 2.0 — תוכנית פעולה ומה נבנה
*עודכן: 2 ביולי 2026 · משלים את TEAM-AUDIT.md*

## הארכיטקטורה החדשה — מי רץ איפה

| רכיב | רץ איפה | על חשבון מה | תפקיד |
|------|---------|-------------|--------|
| **נועם** (הבוט למשתמשים) | Vercel (production) | API (קרדיטים) | חייב API — משרת משתמשים אחרים |
| **רוני (Reliability)** = health-check | Vercel cron, כל 10 דק׳ | חינם כמעט (1 טוקן לבדיקה) | מתריע במייל על תקלה תוך 10 דק׳ |
| **תמר** = סטנדאפ v2 | Vercel cron, יומי 06:00 | ~$0.05/יום | digest נתונים אמיתי + ניסוח |
| **יואב** — טריאז' ותיקוני קוד | Cowork, יומי 08:30 | **המנוי שלך ($100)** | לוגים → תיקון → commit מוכן ל-push |
| **שירה** — שיווק שבועי | Cowork, ראשון 10:00 | **המנוי שלך** | 3 חומרים → Google Drive לאישורך |
| **אריאל** — סגירת שבוע | Cowork, שישי 12:00 | **המנוי שלך** | בדיקות, release notes, סטטוס |
| **דנה** — תמיכה | Vercel (כמו היום) | API (זול, לפי פנייה) | ממשיכה לענות אוטומטית |

**עיקרון:** מה שנוגע בקוד/קבצים/דרייב רץ ב-Cowork על המנוי. מה שחייב לרוץ 24/7 בענן (התראות, נועם, דנה) נשאר ב-Vercel.

## מה נבנה היום (מוכן, מחכה ל-push + SQL)

1. **`app_events`** — טבלת אירועים: כל כשל במערכת נרשם (מקור, קוד, סיבה). זו התשתית שכל הצוות "רואה" דרכה. (migration 0012)
2. **`/api/health`** — בדיקת DB + Anthropic כל 10 דקות. תקלה → מייל אליך תוך 10 דקות (עם זיהוי ספציפי של "נגמר אשראי"). חזרה לתקינות → מייל "הכל ירוק". התראות מוגבלות לאחת כל 45 דק׳.
3. **סטנדאפ v2** — במקום 5 דיווחים מומצאים: digest אמיתי (מטריקות + דלתא יומית, שגיאות, שיחות תקועות, פניות פתוחות, drop-off) + דגלים אוטומטיים + ניסוח אחד של תמר. אם ה-API נופל — ה-digest הגולמי עדיין נשלח, מדויק.
4. **רישום שגיאות** בכל נתיבי הייעוץ (start/turn/finalize) → מזין את הסטנדאפ ואת יואב.
5. **תיקיית Drive** — "GenerAgent Marketing": https://drive.google.com/drive/folders/1a3ADJ2V2gbQcUPRoDcxW_iE1Zi_msn51
6. **3 משימות Cowork מתוזמנות** — יואב (יומי 08:30), שירה (א׳ 10:00), אריאל (ו׳ 12:00).

## פעולות שנשארו לך (5 דקות)

1. **SQL** — להריץ ב-Supabase SQL Editor את `supabase/migrations/0012_app_events.sql` (בלי זה הסטנדאפ v2 וה-health לא יעבדו מלא).
2. **Push** — `cd ~/Documents/GenerAgent && git push`
3. **"Run now"** על כל אחת מ-3 המשימות המתוזמנות (סרגל צד → Scheduled) — כדי לאשר להן כלים פעם אחת ואז ירוצו חלק.
4. חשוב לדעת: משימות Cowork רצות **כשהאפליקציה פתוחה**. אם המחשב כבוי ב-08:30 — יואב ירוץ בפתיחה הבאה. ההתראות הקריטיות (health) לא תלויות בזה — הן ב-Vercel.

## מה הלאה (שלב 2 — אחרי שבוע של דאטה)

- **תמר מציעה sprint שבועי** מתוך ה-digest המצטבר (משימות מתועדפות לאישורך).
- **שחרור אוטומטי של שיחות תקועות** (analyzing > 10 דק׳ → retry finalize).
- **דנה** מקבלת כלי FAQ: מזהה פנייה חוזרת ומציעה עדכון.
- **שירה** מוסיפה חומרים ויזואליים (Canva/Express) לתיקיית הדרייב.
- **Deploy key ליואב** — אם תרצה שיואב גם ידחוף לבד (עכשיו הוא מכין commit ואתה דוחף) — נגדיר SSH key ייעודי עם הרשאה מוגבלת. החלטה שלך.

## מה עם TEAM.md הישן?
נשאר כתיעוד הפרסונות והטון. הסעיפים על "סטנדאפ כל 48 שעות" ו"ping בין סוכנים בזמן אמת" כבר לא משקפים — הארכיטקטורה בקובץ הזה גוברת.
